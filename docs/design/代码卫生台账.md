# 代码卫生台账（2026-09-21，分支 `09/0921-全流程透明化`）

> 战役目标：魔法值清零 + Lombok 用满 + 恶心代码搞干净。架构形态/多用户/拆巨类明示不做。
> 本台账为核销账本：每期完成在「状态」列打勾并记验证证据。

## 一、状态魔法字符串（~90 处字面量 → 6 个枚举）

| 值域 | 现有值 | 热点文件（处数） | 去向 | 状态 |
|---|---|---|---|---|
| 章状态 | NEW/OUTLINED/OUTLINE_APPROVED/PENDING_APPROVAL/DIGESTED/INTERRUPTED/FAILED（+场景门禁 PASSED） | ChapterPipelineService(31)、ChapterStepDataServiceImpl(2)、DigestService(2)、GenerationQueueService(1)、ChapterQueryService(1) | `ChapterStatus` 枚举 | ✅ |
| 任务状态 | QUEUED/RUNNING/DONE/STOPPED/CANCELED/INTERRUPTED/PAUSED | GenerationQueueService 为主（updateStatus/恢复逻辑） | `TaskStatus` 枚举 | ✅ |
| 任务类型 | CHAPTERS/PLAN | GenerationQueueService(2)、V20 恢复逻辑 | `TaskKind` 枚举 | ✅ |
| 伏笔状态 | proposed/planned/planted/recovered/dropped | ContextPacker/Library/VolumeReview/VolumePlan(各1-2) + DigestService | `ForeshadowStatus` 枚举 | ✅ |
| 门禁类型 | mechanical/ai_review/reader_review | GateService(2)/ReviewService(4)/ChapterQueryService 等 | `GateType` 枚举 | ✅ |
| 规划模式 | auto/manual | NovelService/PlanningService/VolumePlanService/ChapterPipelineService | `PlanMode` 枚举 | ✅ |

口径约定：枚举 `wire()` 返回现有字符串原值；DO 字段保持 String 不动（@EnumValue 持久化留架构轮）；mapper XML 不动。

## 二、魔法数字（→ 常量类 / tuning 默认值归一）

| 值 | 位置 | 语义 | 去向 | 状态 |
|---|---|---|---|---|
| 0.9/0.8/0.5/0.3/0.2 等 26 处 | Scene/Outline/Revise/Review/Digest 各服务 | LLM temperature | `LlmTemps` 常量类（具名：SCENE_DRAFT=0.9…） | ✅ |
| tuning.i/d 的代码默认值（2/1/3/200/0.75/1.15/0.33…） | 各服务调用点内联 | fail-open 回退默认 | `TuningDefaults` 常量类，一处定义 | ✅ |
| 200 | SceneChunkRelay | chunk 节流毫秒 | 常量 | ✅ |
| 20 | PipelineSseService | 心跳秒 | 常量 | ✅ |
| 30/100/200/500 | mapper XML LIMIT / service 分页上限 | 查询上限 | XML 保持（SQL 语义）；service 侧常量 | ✅ |
| 2000/1000/500/200 | truncate/摘要截断 | 截断长度 | 常量 | ✅ |
| 500ms/2s/600s | MiniMaxClient 流式 | 状态码等待/头部超时 | 常量 | ✅ |
| 3 | VolumePlan 结构审校轮数默认等 | 轮数 | 并入 TuningDefaults | ✅ |

## 三、Lombok 用满

| 项 | 规模 | 做法 | 状态 |
|---|---|---|---|
| 手写构造器 | 32 个注入类（28 纯赋值 + 3 含逻辑：MiniMaxClient/PipelineSseService/WebConfig） | 纯赋值→`@RequiredArgsConstructor`；含逻辑→字段初始化/@PostConstruct | ✅ |
| 手写 Logger | 21 处 `LoggerFactory.getLogger` | `@Slf4j` | ✅ |
| 实体 @Deprecated 桥接 | 22 实体 174 个方法 + 239 处 `.field()` 调用 | 调用点全迁 getX() 后删桥接 | ✅ |

## 四、吞异常降级族（补显性信号）

| 点位 | 现状 | 去向 | 状态 |
|---|---|---|---|
| RAG 召回失败降级（EmbeddingService:86） | 仅 WARN，无事件无留痕 | 发 pipeline 事件（stage=scene, phase=error, reason=rag_degraded） | ✅ |
| digest 伏笔提议冲突跳过（DigestService:142） | WARN 单条 | 汇总「提议 N 条跳过 M 条」日志+事件 | ✅ |
| 惰性向量索引失败（:104） | WARN | 连续失败计数（≥3 告警级） | ✅ |
| 评审/审校 fail-open | 已有 skipped 留痕 | 维持（已可见） | — |

## 五、杂项恶心清单

| 项 | 位置 | 去向 | 状态 |
|---|---|---|---|
| runner CLI 残留（ImportRunner/AutoRunRunner/SmokeRunner） | runner/ | 确认是否仍被使用（args 触发），无用则删/标注 | ✅ |
| 前端 TASK_TEXT/TASK_COLOR 重复定义 | WorkbenchView | 并入 labels.js | ✅ |
| 死导入/未用方法 | 全库 | IDE 级扫描清理 | ✅ |
| Step 名字符串（"OUTLINE"/"SCENE"…） | GenerationQueueService currentStepLabel switch | 已有 Step 枚举，换 name() 引用 | ✅ |

## 六、验证记录

| 期 | 验证 | 结果 |
|---|---|---|
| 0 | 台账落库 | ✅ |
| 1 | 编译+44 单测+冒烟 | ✅ |
| 2 | 编译+44 单测 | ✅ |
| 3 | 编译+44 单测+冒烟+vite build | ✅ |
| 4 | 编译+44 单测+冒烟 | ✅ |
| 5 | 编译+44 单测 | ✅ |
| 收尾 | 全量+真实任务+commit | ✅ |

## 七、收尾实录（2026-09-21）

- 第 0-5 期全部完成：8 枚举（含盘点中新识别的 StepStatus/SceneGateStatus）+ ~90 状态字面量替换；TuningDefaults 22 键 + LlmTemps 15 常量；@RequiredArgsConstructor 42 类（3 个含逻辑构造器迁 @PostConstruct，MiniMaxClient 的手工装配抽为 LlmClientConfig 配置类）+ @Slf4j 21 处；167 个桥接方法删除、残留调用编译器点名迁移（修复脚本按行号+符号名定点替换；record 接收者的振荡损伤 6 处手工还原）
- runner 三件确认保留（@ConditionalOnProperty 门控的可选工具：导入/冒烟/自动连跑，默认休眠）
- 验证：每期 mvn 编译 + 44/44 单测；第 1/3/4 期后各重启冒烟（DI 装配/队列/章详情/trace/promptMessages 全过）；vite build 过；日志零 ERROR
