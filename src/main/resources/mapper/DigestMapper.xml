<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzdzz.novelgen.dao.DigestMapper">

    <update id="updateContent">

                UPDATE digests SET content_md=#{contentMd}, facts=#{factsJson}::jsonb, update_time=NOW()
                WHERE id=#{id} AND is_deleted=false
                
    </update>

    <select id="findRecent" resultType="string">

                SELECT d.content_md FROM digests d
                JOIN chapters c ON c.id = d.chapter_id
                WHERE c.novel_id=#{novelId} AND c.chapter_no &lt; #{beforeChapter} AND d.is_deleted=false
                ORDER BY c.chapter_no DESC LIMIT #{n}
                
    </select>

    <update id="insert">
INSERT INTO digests (chapter_id, content_md, facts) VALUES (#{chapterId}, #{contentMd}, #{factsJson}::jsonb)
    </update>

    <select id="existsByChapter" resultType="boolean">
SELECT COUNT(*) FROM digests WHERE chapter_id=#{chapterId} AND is_deleted=false
    </select>

    <select id="listByNovel" resultType="map">
                SELECT d.id, c.chapter_no, d.content_md, d.facts::text AS facts, d.update_time
                FROM digests d JOIN chapters c ON c.id = d.chapter_id
                WHERE c.novel_id=#{novelId} AND d.is_deleted=false
                ORDER BY c.chapter_no
    </select>
</mapper>
