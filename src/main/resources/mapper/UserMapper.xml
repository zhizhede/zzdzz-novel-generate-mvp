<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzdzz.novelgen.dao.UserMapper">

    <select id="findAliveByUsername" resultType="com.zzdzz.novelgen.model.dto.UserDTO">

                SELECT id, username, password_hash, role, is_deleted
                FROM users WHERE username=#{username} AND is_deleted=false
                
    </select>

    <select id="findIdByUsername" resultType="long">
SELECT id FROM users WHERE username=#{username} AND is_deleted=false
    </select>

    <select id="insert" resultType="long">
INSERT INTO users (username, password_hash, role) VALUES (#{username}, #{passwordHash}, #{role}) RETURNING id
    </select>
</mapper>
