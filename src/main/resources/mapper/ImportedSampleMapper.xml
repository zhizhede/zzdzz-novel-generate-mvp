<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzdzz.novelgen.dao.ImportedSampleMapper">

    <select id="insert" resultType="long">
INSERT INTO imported_samples (title, genre, chunks, total_chars, source, analysis)
VALUES (#{title}, #{genre}, #{chunks}, #{totalChars}, #{source}, #{analysis}::jsonb)
RETURNING id
    </select>

    <update id="updateTags">
UPDATE imported_samples SET tags=#{tagsJson}::jsonb, update_time=NOW()
WHERE id=#{sampleId} AND is_deleted=false
    </update>
</mapper>
