<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzdzz.novelgen.dao.VolumeReviewMapper">

    <update id="upsert">

                INSERT INTO volume_reviews (novel_id, vol_no, report)
                VALUES (#{novelId}, #{volNo}, #{report.toString()}::jsonb)
                ON CONFLICT (novel_id, vol_no) WHERE is_deleted = FALSE
                DO UPDATE SET report = EXCLUDED.report, update_time = NOW()
                
    </update>

    <select id="findJson" resultType="string">

                SELECT report::text FROM volume_reviews
                WHERE novel_id = #{novelId} AND vol_no = #{volNo} AND is_deleted = FALSE
                
    </select>
</mapper>
